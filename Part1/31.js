// use const for creating array

// heap memory ["apple", "mango"] 0x11

// const fruits = ["apple", "mango"]; // 0x11
// fruits.push("banana");//here memory adress is not changed hence o error...error will occur if we directly change fruits=["grape"];
// console.log(fruits);

//most developers use const to define array(or any reference type) instead of let since any change could be made usig let